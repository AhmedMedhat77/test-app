// import { createAsyncThunk } from "@reduxjs/toolkit";
// import { ActionTypes } from "../../ActionTypes";

export default { a: 0 };
// export function makeCustomAsyncThunk<T>(
//   prefix: ActionTypes,
//   fn: (params: T) => any
// ) {
//   return createAsyncThunk(
//     prefix,
//     async function (arg: { params: T; callback?: (resp) => void }) {

//     }
//   );
// }
